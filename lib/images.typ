#let heig-vd-baseline-logo(height: 2cm, width: auto) = image(
  "../img/heig-vd-baseline.svg",
  height: height,
  width: width,
)
#let heig-vd-logo(height: 2cm, width: auto) = image("../img/heig-vd.svg", height: height, width: width)
#let hes-so-logo(height: 1.5cm, width: auto) = image("../img/hes-so.svg", height: height, width: width)

#let heig-vd-baseline-logo-sm = heig-vd-baseline-logo(height: 1cm)
#let heig-vd-logo-sm = heig-vd-logo(height: 1cm)
#let hes-so-logo-sm = hes-so-logo(height: 0.8cm)

#let heig-vd-baseline-logo-lg = heig-vd-baseline-logo(height: 3cm)
#let heig-vd-logo-lg = heig-vd-logo(height: 3cm)
#let hes-so-logo-lg = hes-so-logo(height: 2.5cm)
